import { echo } from "io"
struct Point {
  x: number;
  y: number;
}

fn (p Point) magnitude() number {
  const d = ((p.x * p.x) + (p.y * p.y)).sqrt()
  const v = unwrap(d) or { 0 }
  return v
}

const p = Point { x: 3, y: 4 }
echo(string(p.magnitude()))
