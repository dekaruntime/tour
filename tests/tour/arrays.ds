import { echo } from "io"
fn sum(values: Array<number>) number {
  let total = 0
  for (const v of values) {
    total += v
  }
  return total
}

let values = [1, 2, 3]
values[0] = 10
echo(string(values.length))
echo(string(sum(values)))
