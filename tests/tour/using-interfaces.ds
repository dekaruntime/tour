import { echo } from "io"
interface Named {
  mut name: string;
}

struct Person {
  name: string;
}

fn rename(n: Named, newName: string) void {
  n.name = newName
}

let p = Person { name: "Old" }
rename(p, "New")
echo(p.name)
