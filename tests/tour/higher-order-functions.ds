import { echo } from "io"
fn apply(n: number, f: fn(number) number) number {
  return f(n)
}

fn makeMultiplier(factor: number) fn(number) number {
  return fn(n: number) number { return n * factor }
}

const triple = makeMultiplier(3)
echo(string(apply(7, triple)))
