import { echo } from "io"
struct Point {
  x: number;
  y: number;
}

fn (p mut Point) translate(dx: number, dy: number) void {
  p.x = p.x + dx
  p.y = p.y + dy
}

let p = Point { x: 1, y: 2 }
p.translate(3, 4)
echo(string(p.x + p.y))
