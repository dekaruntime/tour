import { echo } from "io"
async fn delayed() Promise<number> {
  return 42
}

async fn main() Promise<void> {
  const promise = delayed()
  const value = await promise
  echo(string(value))
}

main()
