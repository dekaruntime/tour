enum Shape {
  Empty,
  Rect(number),
}

fn describe(s: Shape | number) string {
  return match (s) {
    Shape.Empty => "empty",
    Shape.Rect(n) => "rect " + string(n),
    number(n) => string(n),
  }
}

const rectangle = describe(Shape.Rect(3))
const scalar = describe(5)
