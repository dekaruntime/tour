import { echo } from "io"
enum Status {
  Loading,
  Ready,
  Failed,
}

const current = Status.Ready

const label = match (current) {
  Loading => "Loading...",
  Ready => "Ready",
  Failed => "Failed",
  _ => "Unknown",
}

echo(label)
