struct Point {
  x: number
  y: number
}

enum Message {
  Move(Point),
  Stop,
}

const message = Message.Move(Point { x: 7, y: 9 })
const x = match (message) {
  Move(Point { x }) => x,
  Stop => 0,
}
