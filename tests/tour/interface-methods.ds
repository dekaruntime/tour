import { echo } from "io"
interface Displayable {
  fn display() string;
}

struct Person {
  name: string;
}

fn (p Person) display() string {
  return p.name
}

fn show(d: Displayable) void {
  echo(d.display())
}

const person = Person { name: "DekaScript" }
show(person)
