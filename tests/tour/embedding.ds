import { echo } from "io"
struct Person {}

fn (p Person) greet() string {
  return "Hello"
}

struct Employee {
  Person;
  name: string;
}

fn (e Employee) greet() string {
  return "Hi, " + e.name
}

const e = Employee { name: "DekaScript" }
echo(e.greet())
