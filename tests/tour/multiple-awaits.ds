import { echo } from "io"
async fn step(n: number) Promise<number> {
  return n * 2
}

async fn main() Promise<void> {
  const a = await step(1)
  const b = await step(a)
  const c = await step(b)
  echo(string(c))
}

main()
