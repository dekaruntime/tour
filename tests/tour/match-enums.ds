import { echo } from "io"
enum Status {
  Loading,
  Ready,
  Failed,
}

const current = Status.Ready
const label = match (current) {
  Loading => "loading",
  Ready => "ready",
  Failed => "failed",
  _ => "other",
}
echo(label)
